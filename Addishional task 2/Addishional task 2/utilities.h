#pragma once
#include <stdbool.h>

//swaps two elements
void swap(char* a, char* b);

//copy string
//source - string that would be copied
char* copyStr(char const* source);

//checks if it is number
bool isNumber(char* number);