#pragma once
#include <stdbool.h>

// checks if first string is bigger than the second
bool firstBiggerThanSecond(char const* str1, char const* str2);

// copies string
char* copyStr(char const* source);